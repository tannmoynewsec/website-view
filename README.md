# website-view

This repository hosts a simple static `index.html` page embedding the provided SharePoint video iframe.

## Embedded content

- Title: `NeswecITTeamDiscuss.MOV`
- Embed source: Microsoft SharePoint (`strongholdinvest-my.sharepoint.com`)

## Usage

Open `index.html` directly in a browser, or publish the repository with GitHub Pages to access it online.
